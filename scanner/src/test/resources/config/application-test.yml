spring:
  application:
    name: flow-scanner

scanner:
  kafka-replica-set: ${kafka.hosts}
  environment: test
  default-item-collection:
    id: "A.ebf4ae01d1284af8.RaribleNFT.NFT"
    owner: "0xebf4ae01d1284af8"
    name: "RaribleNFT"
    symbol: "RNFT"

grpc:
  client:
    flow:
      address: dns:///flow-testnet.g.alchemy.com:443
      enable-keep-alive: true

blockchain:
  scanner:
    flow:
      chainId: TESTNET
