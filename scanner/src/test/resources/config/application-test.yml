spring:
  application:
    name: flow-scanner

app:
  chain-id: TESTNET
  kafka-replica-set: ${kafka.hosts}
  environment: test

grpc:
  client:
    flow:
      address: dns:///flow-testnet.g.alchemy.com:443
      enable-keep-alive: true

blockchain:
  scanner:
    flow:
      chainId: TESTNET
