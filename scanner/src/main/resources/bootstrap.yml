spring:
  jmx:
    enabled: false
  cloud:
    service-registry:
      auto-registration:
        enabled: true
    consul:
      host: consul-agent
      port: 8500
      config:
        watch:
          enabled: false
        enabled: true
        prefixes:
